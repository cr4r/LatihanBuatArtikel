<div class="container">
    <h1 class="mb-1"><%= artikel.title %></h1>
    <div class="text-muted mb-2">
      <!-- Menampilkan Tanggal -->
      <%= artikel.createdAt.toLocaleDateString() %>
    </div>
    <a href="/" class="btn btn-secondary">Home</a>
    <a href="/edit/<%= artikel.id %>" class="btn btn-info">Edit</a>

    <div>
      <!-- Menampilkan isi dari artikel -->
      <%- artikel.isi %>
    </div>
</div>